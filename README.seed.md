# 关于依赖

本项目演示直接复用友号 [@c991china/python-utils](https://github.com/c991china/python-utils)
的 `tree()` 工具：在 `src/main.py` 里 `from python_utils import tree`。

安装友号库后即可运行：

```bash
pip install git+https://github.com/c991china/python-utils.git
python -m src.cli --name seed
```
